# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Test, lint, and type-check caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.pyre/
.hypothesis/
.coverage
.coverage.*
htmlcov/
coverage.xml

# Virtual environments
.venv/
venv/
.uv-cache/

# Local configuration
.env
.env.*
!.env.example
!.env.template

# Notebook checkpoints
.ipynb_checkpoints/

# Local datasets and generated results (see the exceptions at the bottom)
/data/*
/outputs/

# Editor and OS files
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db
Desktop.ini

# Keep uv.lock and .python-version tracked for reproducible environments.

# Data & model artefacts live on the VM, never in git (root data/ only; src/turbine_tslm/data is code)
!/data/README.md
*.zip
*.parquet
*.zarr
checkpoints/
runs/
wandb/
/outputs/
*.pt
*.safetensors
*.ckpt
.ipynb_checkpoints/

# Nebius internal guides (do not distribute)
hackathon-25-bypass.pdf
Serverless-hackathon-guide.pdf

# Local tooling state. The shared hook configs (.claude/settings.json, .codex/hooks.json, .gemini/settings.json,
# .entire/settings.json) are tracked on purpose so the team's Entire session tracking works; only local state is ignored.
.claude/settings.local.json
.codex/*.local.json
.gemini/settings.local.json
.devin/
.env

# Removed from the public repo (third-party / organiser material, private notes); local copies may remain on disk
brief.pdf
ts-corpus-list.md
docs/opentslm-paper/
docs/notes/
docs/session-handoff.md

# Committed on purpose: data/README.md and the two preprocessed window tables (28 + 19 MB) for local training
!/data/README.md
!/data/interim/
/data/interim/*
!/data/interim/*_windows.parquet
