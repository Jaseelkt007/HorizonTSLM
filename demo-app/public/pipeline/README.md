Copy of `replay/` (the standalone pipeline console). `replay/` is the source; after editing it, copy
`index.html` and the three case JSONs here again. Served at `/pipeline/index.html`; the app embeds it with
`?embed&theme=dark`.
