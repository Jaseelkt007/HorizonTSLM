"""TimeNet connectors for the Cubico wind farms (`cubico/penmanshiel`, `cubico/kelmarsh`).

One code path, two dataset cards. Layout follows timenet-connectors: <org>/<name>/{connector.py, dataset.yaml,
requirements.txt}. How a connector outside the timenet_connectors package is discovered by `timenet-build`
is still to be confirmed with the organisers (see CLAUDE.md, open questions).
"""
